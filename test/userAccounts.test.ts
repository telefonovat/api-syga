/*
 * Cases needed testing
 *    #Registration
 *      - Duplicate username
 *      - No password
 *      - Email verification
 */

describe('Registration', () => {
  beforeAll(() => {});
  it('Duplicate username fails', () => {});
});
